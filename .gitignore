# OS / editor noise
.DS_Store
Thumbs.db
*.lnk
.idea/
.vscode/
*.swp

# Node
node_modules/

# OpenCode local helper dirs (skills live globally in ~/.opencode/skills)
**/.opencode/

# PHP / Laravel
vendor/

# Environment — never commit secrets
.env
.env.*
!.env.example

# Build / cache artifacts
dist/
dist-ssr/
.phpunit.cache/
